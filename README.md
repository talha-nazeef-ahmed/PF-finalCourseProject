It is a SIEM (Security Information and Event Management) prototype. For now it takes logs as inputs from the user, processes it, stores it in RAM for the current sesssion via 2D arrays.
For permanent storage it uses files system. It has multiple functionalities like displaying logs, sorting logs based on keywords, datetime, severity, etc, showing statistics, checking for anamolies and much more.
